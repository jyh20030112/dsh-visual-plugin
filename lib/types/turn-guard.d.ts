/** Whether a tool call belongs to the turn that first admitted an attachment. */
export declare function isSameTurnAttachmentToolCall(events: readonly unknown[], attachmentId: string, callId: string): boolean;
